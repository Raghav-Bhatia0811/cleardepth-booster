[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cleardepth"
version = "0.1.0"
description = "Stereo depth estimation for transparent objects"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["cleardepth*"]